services:
  - type: web
    name: superset
    env: docker
    plan: free
    branch: main_analysis
    dockerfilePath: ./Dockerfile
    envVars:
      - key: PYTHONPATH
        value: /app/pythonpath
    healthCheckPath: /
    autoDeploy: true
